import React from "react";
import "./index.scss";

const Spinner = () => (
    <div className="spinner">
        <div className="spinner-inside">
            <div />
        </div>
    </div>
);

export default Spinner;
